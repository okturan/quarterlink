{
	"name": "quarterlink",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"check": "npm run cf-typegen && tsc --noEmit && wrangler deploy --dry-run",
		"smoke:browser": "node scripts/smoke-browser.mjs",
		"smoke:prod": "node scripts/smoke-production.mjs",
		"test": "node --test tests/*.test.mjs"
	},
	"devDependencies": {
		"@types/node": "^26.1.1",
		"typescript": "^5.5.2",
		"wrangler": "^4.111.0"
	}
}
